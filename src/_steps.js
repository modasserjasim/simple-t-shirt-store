/* Context Api setup:

1. Declare a context: const RingContext = createContext('initial value);
2. set provider with value
3. useContext in the place where you want to get the context using useContext(MyContext)

*/